# Write tests
1. Read the code under test. Identify the contract and edge cases (empty, invalid, large, boundary).
2. Write tests with `write_file` using the project's framework. Run them with `shell`.
3. Make them pass. Report coverage of the important paths.
