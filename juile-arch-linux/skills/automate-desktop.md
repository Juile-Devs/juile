# Automate a desktop task (computer use)
1. `computer` action=screenshot to SEE the screen and exact coordinates.
2. Use open_app to launch/focus apps. Click/type with `computer` using integer x,y from the screenshot.
3. Re-screenshot every couple of actions — the layout moves. Verify the end state.
