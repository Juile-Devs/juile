# Design clear data viz
Pick the right chart (trend->line, parts->bar/stacked, share->donut sparingly, correlation->scatter). Label directly,
drop chart junk, one accent + greys, sort meaningfully, annotate the insight. Use the chart widget.
