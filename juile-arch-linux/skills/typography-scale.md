# Typography that reads premium
One display font + one clean sans for body (or a single great variable font). Modular scale (~1.25). Line-height 1.5
for body, tighter for headings. Max line length ~70ch. Real hierarchy via size + weight + color, not just bold.
