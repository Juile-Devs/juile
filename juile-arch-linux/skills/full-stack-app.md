# Ship a full-stack app
1. Plan: pages, data model, API, auth. Pick a lean stack and `make_dir` the tree.
2. Backend (API + DB), then frontend (designed UI). Wire them. Seed sample data.
3. Run it, fix every error, verify the core flow end to end. Ship a README + run command.