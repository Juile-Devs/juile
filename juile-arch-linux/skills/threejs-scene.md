# Build a Three.js scene
Use the sim widget (a full HTML doc). Scene + perspective camera + renderer; soft lighting; a hero mesh with subtle
auto-rotate; OrbitControls; resize handler; requestAnimationFrame loop. Tasteful, performant, no clutter.
