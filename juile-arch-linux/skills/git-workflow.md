# Git workflow
Use `shell` (git). 1. `git status` + `git diff` to see changes. 2. Stage only what's relevant.
3. Commit with a clear message. 4. Never force-push or rewrite shared history unless asked.
