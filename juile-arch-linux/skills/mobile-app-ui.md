# Design a mobile app screen
Thumb-reachable primary actions (bottom). 44px+ touch targets. Bottom tab bar, sticky header, generous padding.
Big readable type, clear hierarchy, one accent. Safe-area aware. Smooth 200ms transitions between states.
