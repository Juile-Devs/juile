# Scaffold a new project
1. Pick a minimal stack and state assumptions. `make_dir` the structure.
2. Create files with `write_file`. Add a README and a run command.
3. Install deps and run it with `shell`. Show it working.
