# Design forms people finish
One column, top-aligned labels, logical grouping, inline validation, helpful placeholder + helper text, clear primary
button. Ask only what you need. Show progress for long forms. Big touch targets.
