# Refactor code safely
1. Read the target with `read_file`. Make sure tests exist; if not, write one that pins current behavior.
2. Refactor in small steps with `write_file`, re-running tests after each.
3. Don't change behavior. Keep the diff minimal. Summarize what improved.
