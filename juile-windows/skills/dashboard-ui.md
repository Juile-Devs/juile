# Design a data dashboard
1. Layout: left nav, top bar (search + user), a grid of cards. F-pattern; most important metric top-left.
2. KPI cards (big number + delta + sparkline), a primary chart, a data table with sort/filter, status badges.
3. Restrained palette, one accent for emphasis, lots of whitespace, tabular numerals. Render charts with the chart widget.
