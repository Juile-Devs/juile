# Responsive layout
Mobile-first. CSS grid with minmax/auto-fit for cards; flex for rows. Fluid type with clamp(). Breakpoints by content,
not devices. Test 360px, 768px, 1280px. Nothing should overflow or cramp.
