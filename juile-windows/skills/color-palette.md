# Build a color palette
Pick ONE accent. Generate a neutral ramp (50->900). Add semantic colors (success/warn/danger/info). Check contrast
(>=4.5:1 for text). Define light + dark tokens. Use the accent sparingly — for emphasis, not everywhere.
