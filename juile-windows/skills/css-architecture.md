# Clean CSS architecture
Tokens as CSS variables on :root. Component-scoped classes, low specificity, no !important. Utility helpers for spacing.
Prefer grid/flex over hacks. Keep animations to transform/opacity. Mobile-first media queries.
