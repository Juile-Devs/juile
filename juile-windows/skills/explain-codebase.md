# Explain a codebase
1. `list_dir` the root, read the README and entry points.
2. Trace the main flow across files with `read_file`.
3. Produce a short map: structure, key modules, data flow, where to change what.
