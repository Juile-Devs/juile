# Generate an image
ONE `mcp` call: server=higgsfield, tool=generate_image, params.model=nano_banana_pro, a vivid prompt,
and an aspect_ratio. The image card appears automatically — just confirm in one line.
